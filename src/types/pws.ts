export type DeviceArgs = {
    macAddress: string,
    apiKey: string,
}

export type Device = {
    macAddress: string,
    apiKey: string,
    userId: string,
    id: string,
}

export type DeviceInfo = {
    macAddress: string,
    lastData: any,
    info: any,
}

