import React from 'react'

import ForecastDiscussion from '../ForecastDiscussion'

const Forecast = (p: any) => {
    console.log({ p })
    return (
        <div>
            <ForecastDiscussion/>
        </div>
    )
}

export default Forecast
